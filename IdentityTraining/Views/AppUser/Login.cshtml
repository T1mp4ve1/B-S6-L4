<div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card shadow-sm" style="width: 22rem;">
        <div class="card-body">
            <h3 class="card-title text-center mb-4">Accedi</h3>
            <form method="post" action="/AppUser/Login">
                <!-- Username -->
                <div class="mb-3">
                    <input type="text" class="form-control" name="Email" placeholder="Inserisci il tuo username" required>
                </div>
                <!-- Password -->
                <div class="mb-3">
                    <input type="Password" class="form-control" name="Password" placeholder="Inserisci la tua password" required>
                </div>
                <!-- Submit button -->
                <div class="d-grid">
                    <button type="submit" class="btn btn-primary">Login</button>
                </div>
            </form>
        </div>
    </div>
</div>